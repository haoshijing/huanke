<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huanke.iot.base.dao.device.typeModel.DeviceModelAblityOptionMapper">

    <sql id="tableName">
        t_device_model_ablity_option
    </sql>
    <!-- 插入一条记录 -->
    <insert id="insert"  parameterType="com.huanke.iot.base.po.device.typeModel.DeviceModelAblityOptionPo">
        INSERT INTO <include refid="tableName"/>
        (
        modelAblityId,
        ablityOptionId,
        definedName,
        minVal,
        maxVal,
        status,
        createTime
        )
        VALUES (
        #{modelAblityId},
        #{ablityOptionId},
        #{definedName},
        #{minVal},
        #{maxVal},
        #{status},
        #{createTime}
        )
    </insert>
    <update id="updateById" parameterType="com.huanke.iot.base.po.device.typeModel.DeviceModelAblityOptionPo">
        update <include refid="tableName"/>
        <set>
            <if test = "modelAblityId != null">
                modelAblityId = #{modelAblityId},
            </if>
            <if test = "ablityOptionId != null">
                ablityOptionId = #{ablityOptionId},
            </if>
            <if test = "definedName != null and definedName != ''  ">
                definedName = #{definedName},
            </if>
            <if test = "minVal != null ">
                minVal = #{minVal},
            </if>
            <if test = "maxVal != null ">
                maxVal = #{maxVal},
            </if>
            <if test = "status != null ">
                status = #{status},
            </if>
            lastUpdateTime = #{lastUpdateTime}
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from <include refid="tableName"/>
        where id = #{id}
    </delete>

    <select id="selectById" parameterType="java.lang.Integer" resultType="com.huanke.iot.base.po.device.typeModel.DeviceModelAblityOptionPo">
        select
        *
        FROM <include refid="tableName"/>
        WHERE id = #{id} limit 1
    </select>

    <select id = "selectCount" resultType="java.lang.Integer">
        select count(1)
        from <include refid="tableName"/>
        <where>
            <if test = "param.modelAblityId != null">
                and modelAblityId = #{param.modelAblityId}
            </if>
            <if test = "param.definedName != null and param.definedName != '' ">
                and definedName = #{param.definedName}
            </if>
            <if test = "param.status != null">
                and status = #{param.status}
            </if>
        </where>
    </select>


    <select id="selectList" resultType="com.huanke.iot.base.po.device.typeModel.DeviceModelAblityOptionPo">
        select * from <include refid="tableName"/>
        <where>

            <if test = "param.modelAblityId != null">
                and modelAblityId = #{param.modelAblityId}
            </if>
            <if test = "param.ablityOptionId != null">
                and ablityOptionId = #{param.ablityOptionId}
            </if>
            <if test = "param.definedName != null and param.definedName != '' ">
                and definedName = #{param.definedName}
            </if>
            <if test = "param.status != null">
                and status = #{param.status}
            </if>
        </where>
        limit #{limit} offset #{offset}
    </select>

    <select id="getByJoinId" resultType="com.huanke.iot.base.po.device.typeModel.DeviceModelAblityOptionPo">
        select * from <include refid="tableName"/>
        <where>

            <if test = "modelAblityId != null">
                and modelAblityId = #{modelAblityId}
            </if>
            <if test = "ablityOptionId != null">
                and ablityOptionId = #{ablityOptionId}
            </if>

        </where>
        limit 1
    </select>

    <select id="getOptionsByJoinId" resultType="com.huanke.iot.base.po.device.typeModel.DeviceModelAblityOptionPo">
        select * from <include refid="tableName"/>
        <where>

            <if test = "modelAblityId != null">
                and modelAblityId = #{modelAblityId}
            </if>

        </where>
    </select>

</mapper>