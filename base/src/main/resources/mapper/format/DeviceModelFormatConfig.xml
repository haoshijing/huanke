<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huanke.iot.base.dao.format.DeviceModelFormatConfigMapper">

    <sql id="tableName">
        t_device_model_format_config
    </sql>
    <!-- 插入一条记录 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.huanke.iot.base.po.format.DeviceModelFormatConfigPo">
        INSERT INTO
        <include refid="tableName"/>
        (
        modelId,
        formatId,
        pageId,
        itemId,
        ablityId,
        showStatus,
        showName,
        status,
        createTime
        )
        VALUES (
        #{modelId},
        #{formatId},
        #{pageId},
        #{itemId},
        #{ablityId},
        #{showStatus},
        #{showName},
        #{status},
        #{createTime}
        )
    </insert>

    <select id="selectById" parameterType="java.lang.Integer" resultType="com.huanke.iot.base.po.format.DeviceModelFormatConfigPo">
        select
        *
        FROM
        <include refid="tableName"/>
        WHERE id = #{id} limit 1
    </select>


    <update id="updateById" parameterType="com.huanke.iot.base.po.format.DeviceModelFormatConfigPo">
        update
        <include refid="tableName"/>
        <set>
            <if test="modelId != null">
                modelId = #{modelId},
            </if>
            <if test="formatId != null">
                formatId = #{formatId},
            </if>
            <if test="pageId != null">
                pageId = #{pageId},
            </if>
            <if test="itemId != null">
                itemId = #{itemId},
            </if>
            <if test="ablityId != null">
                ablityId = #{ablityId},
            </if>
            <if test="showStatus != null">
                showStatus = #{showStatus},
            </if>
            <if test="showName != null and showName !='' ">
                showName = #{showName},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            lastUpdateTime = #{lastUpdateTime}
        </set>
        <where>
            1= 1
            <if test="id != null">and id = #{id}</if>
        </where>
    </update>

    <select id="obtainAbilityIdsByJoinId" resultType="java.lang.Integer">
        select
        ablityId
        FROM
        <include refid="tableName"/>
        WHERE modelId = #{modelId}
        AND formatId = #{formatId}
        AND pageId = #{pageId}
        AND itemId = #{itemId}
        AND status = 1
    </select>
</mapper>